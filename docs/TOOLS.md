# Tools Reference

<!-- AUTO-GENERATED by scripts/generate-tools-doc.mjs — do not edit manually -->

50 tools registered.

## analysis-analyze-symbol

| Tool | Description |
|---|---|
| `analyze_symbol` | Full impact analysis of a symbol in ONE call: references (with type classification), callers (who calls it), and callees (what it calls). Use this before refactoring instead of calling find_symbol_references + get_callers + get_callees separately. |

## analysis-find-dependents

| Tool | Description |
|---|---|
| `find_dependents` | Find all files that import a given file (reverse dependency lookup). Useful for understanding impact of changes. |

## analysis-find-imports

| Tool | Description |
|---|---|
| `find_imports` | Extract all imports from a source file with their details (source module, specifiers, import type). Supports TypeScript, JavaScript, Python, Kotlin, and Lua (require/dofile/loadfile). Do NOT use to find who imports a file — use find_dependents instead. |

## analysis-find-related-tests

| Tool | Description |
|---|---|
| `find_related_tests` | Find test files associated with a source file using common naming conventions (*.test.*, *.spec.*, __tests__/*, test_*.py, *_test.py). |

## analysis-find-unused-imports

| Tool | Description |
|---|---|
| `find_unused_imports` | Detect imports that are declared but never used in the file. Useful for code cleanup and reducing bundle size. |

## analysis-get-callees

| Tool | Description |
|---|---|
| `get_callees` | Find all functions/methods called within a function's body (downstream call hierarchy). Answers 'what does this function call?' Do NOT use for a complete symbol analysis — analyze_symbol bundles callers + callees + references in one call. |

## analysis-get-callers

| Tool | Description |
|---|---|
| `get_callers` | Find all locations that call a function/method (upstream call hierarchy). Answers 'who calls this function?' Do NOT use for a complete symbol analysis — analyze_symbol bundles callers + callees + references in one call. |

## analysis-get-file-stats

| Tool | Description |
|---|---|
| `get_file_stats` | Get comprehensive statistics about a source file including line counts (code/blank/comment), symbol counts by kind (functions, classes, etc.), import/export counts. Quick overview of file size and complexity. |

## analysis-get-file-summary

| Tool | Description |
|---|---|
| `get_file_summary` | Get a comprehensive human-readable summary of a source file including stats, structure overview, and key exports. One-stop tool for quickly understanding any source file. |

## directory-allowed

| Tool | Description |
|---|---|
| `list_allowed_directories` | Returns allowed directories based on MCP Roots Protocol. |

## directory-copy

| Tool | Description |
|---|---|
| `chmod` | Change file/directory permissions. Accepts octal (e.g. 0o755, 755) or symbolic (e.g. 'u+x', 'go-w') mode. |
| `copy_file` | Copy a file or directory. Recursive for directories. Rejects existing destinations unless overwrite: true. NOT undoable — the destination is not snapshotted; use delete_file on the copy to revert. |
| `create_symlink` | Create a symbolic link. The link path must not already exist. |

## directory-create

| Tool | Description |
|---|---|
| `create_directory` | Create a directory including any nested parent directories. Idempotent — no error if the directory already exists. |

## directory-delete

| Tool | Description |
|---|---|
| `delete_directory` | Delete a directory. Use recursive=true to delete non-empty directories. Can be undone with filesystem_undo. |
| `delete_file` | Delete a file. Can be undone with filesystem_undo. |
| `delete_path` | Delete a file or directory. Automatically detects type. Use recursive=true for non-empty directories. Can be undone with filesystem_undo. |

## directory-get-info

| Tool | Description |
|---|---|
| `get_file_info` | Get file metadata including size, creation/modification/access dates, file type, and permissions. |

## directory-list

| Tool | Description |
|---|---|
| `directory_tree` |  |
| `list_directory` | List files and directories with [FILE] and [DIR] prefixes. |
| `list_directory_with_sizes` | List files and directories with size information. |

## directory-move

| Tool | Description |
|---|---|
| `move_file` | Move or rename files and directories. Atomic operation — the source path no longer exists after a successful move. NOT undoable — no snapshot is recorded; the source is moved, not copied. Rejects existing destinations unless overwrite: true. |

## directory-watch

| Tool | Description |
|---|---|
| `stop_watching` | Stop watching a directory by watcher ID. |
| `watch_directory` | Watch a directory for changes. Returns a watcher ID. |

## file-read

| Tool | Description |
|---|---|
| `read_media_file` | Read an image or audio file and return base64-encoded data with MIME type. Supports: png, jpg, gif, webp, bmp, svg, mp3, wav, ogg, flac, aac, m4a. |
| `read_multiple_files` | Read multiple files simultaneously. More efficient than reading one by one. |
| `read_text_file` | Read file contents as UTF-8 text. Use 'head' for first N lines, 'tail' for last N lines, 'offset' to start at a 1-based line number (combined with 'head' as window size). Always treats file as text regardless of extension. Do NOT use this to understand a file's structure — use get_file_summary or get_symbols_overview instead (saves context). |

## file-write

| Tool | Description |
|---|---|
| `edit_file` | Make line-based edits to a file. Returns a git-style diff. Do NOT use for renaming symbols or replacing function bodies — use rename_symbol or replace_symbol_body instead (AST-precise, no regex risk). Always dryRun first for complex edits. |
| `write_file` | Create or overwrite a file with new content. |

## search-bulk-rename

| Tool | Description |
|---|---|
| `bulk_rename` | Rename multiple files using pattern matching. NOT undoable — renames are not snapshotted; use dryRun first to preview. |

## search-content

| Tool | Description |
|---|---|
| `search_content` | Search file contents using ripgrep (regex search). |

## search-count

| Tool | Description |
|---|---|
| `count_matches` | Count pattern matches in files using ripgrep. |

## search-diff

| Tool | Description |
|---|---|
| `diff_files` | Compare two files and show differences. |

## search-files

| Tool | Description |
|---|---|
| `search_files` | Search for files by name pattern using ripgrep. Returns matching file paths without reading their contents. |

## search-glob

| Tool | Description |
|---|---|
| `find_by_glob` | Find files using glob patterns (e.g., '**/*.ts'). Supports multiple patterns, ignore rules, and depth filtering. |

## search-patterns

| Tool | Description |
|---|---|
| `get_project_patterns` | Get code patterns from a AGENTS.md file. Returns pattern definitions organized by type (code, structure, config). |

## semantic-find-deprecated-usages

| Tool | Description |
|---|---|
| `find_deprecated_usages` | Detect usages of deprecated symbols across the codebase. Works like VSCode's deprecated linting - finds where @deprecated APIs are being called, not just where they are declared. |

## semantic-find-string-literals

| Tool | Description |
|---|---|
| `find_string_literals` | Find string literals in source code matching a pattern. Useful for finding tool names, config keys, and other string values that aren't code symbols. |

## semantic-find-symbol

| Tool | Description |
|---|---|
| `find_symbol` | Find a symbol by name pattern. Supports patterns like 'MyClass', 'MyClass/myMethod', or wildcards like 'User*'. |

## semantic-find-symbol-references

| Tool | Description |
|---|---|
| `find_symbol_references` | Find all references to a symbol across the codebase. Now includes reference type classification (call, import, type, etc.) and call count. Do NOT use when you only need callers of a function — get_callers is cheaper. For a full symbol analysis (references + callers + callees in one call), use analyze_symbol. |

## semantic-find-unused-symbols

| Tool | Description |
|---|---|
| `find_unused_symbols` | Find exported symbols that have no references across the codebase (dead code detection). Useful for identifying unused functions, classes, and exports. |

## semantic-get-symbols-overview

| Tool | Description |
|---|---|
| `get_symbols_overview` | List all top-level symbols (classes, functions, etc.) in a file. Returns symbol names, kinds, and locations. |

## server-stats-tools

| Tool | Description |
|---|---|
| `get_server_stats` | Get server metrics, cache sizes, undo stack depth, and uptime. Returns counters (tool invocations, errors), histograms (p50/p95 durations), gauges (cache sizes), memory/CPU usage, and config status. |

## undo-tools

| Tool | Description |
|---|---|
| `extract_method` | Extract a range of lines into a new function/method. Analyzes free variables to determine parameters. Inserts the new function after the parent symbol and replaces the extracted lines with a call to the new function. |
| `inline_variable` | Replace all usages of a variable with its initializer value and remove the variable declaration. The variable must have a simple initializer. |
| `introduce_parameter` | Convert a local expression into a function parameter with a default value. Adds the new parameter to the function signature and replaces the expression in the function body with the parameter name. |
| `undo` | Undo the last N destructive filesystem operations (writes, edits, symbol replacements, renames). Restores files to their pre-mutation state. Use filesystem_undo_peek to preview before undoing. |
| `undo_all` | Undo ALL recorded destructive filesystem operations. Restores every file to its state before the session started editing. Use filesystem_undo_peek first to review what will be undone. |
| `undo_peek` | Preview the last N entries on the undo stack without applying them. Shows what each undo operation would restore. |
| `undo_status` | Show the current status of the undo stack and staleness guard. Includes stack depth, staleness guard state, and configuration. |

